name: Deploy Playground

on:
  push:
    branches:
      - main
    paths:
      - 'playground/**'
      - '.github/workflows/deploy-playground.yml'
  workflow_dispatch:

jobs:
  deploy-playground:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'playground/package-lock.json'

      - name: Install dependencies
        working-directory: playground
        run: npm ci

      - name: Build playground
        working-directory: playground
        run: npm run build

      - name: Deploy Playground (placeholder)
        run: |
          echo "=== PLAYGROUND DEPLOYMENT PLACEHOLDER ==="
          echo "Would deploy: Next.js application"
          echo "Port: 3000"
          echo "Environment: production"
          echo "Build artifact: playground/.next/"
          echo "========================================="

      - name: Echo deployment summary
        run: |
          echo "Playground deployment workflow complete (stub)"
